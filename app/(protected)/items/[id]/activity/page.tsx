"use client";

import { StubPage } from "@/components/ui";

export default function ItemActivityPage() {
  return (
    <StubPage title="Activity">
      Edit history, completions, and engine decisions will appear here once the
      activity log is wired up.
    </StubPage>
  );
}
