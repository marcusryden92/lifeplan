"use client";

import { StubPage } from "@/components/ui";

export default function ItemSchedulePage() {
  return (
    <StubPage title="Schedule">
      A per-item schedule view is coming. For now, the Calendar surface owns
      scheduled events end-to-end — see the &ldquo;Next on calendar&rdquo; card
      on Overview for the nearest upcoming block.
    </StubPage>
  );
}
