import { calendarColors } from "@/data/calendarColors";

export const REFERENCE_WEEK_DATE = new Date(2024, 0, 1);
export const UNASSIGNED_COLOR = "#9ca3af";

// Templates share the app-wide calendar palette so colors stay consistent
// with planner tasks and the template popover.
export const TEMPLATE_PALETTE = calendarColors;
