# Precedence — queues (pipes) and dependencies for Planner items

**STATUS (2026-07-14): Phases 1–8 IMPLEMENTED; §4d Detour IMPLEMENTED (2026-07-13), adversarially reviewed and repaired (2026-07-14); §4b Directional Graph view IMPLEMENTED (2026-07-14)** on branch `feature/inter-goal-dependencies`. This note is now a HAND-OFF: §1 records what exists and where, §2 the deviations from the original plan you need to know, §3 the outstanding manual verification, §4 the not-yet-implemented features with their design decisions preserved. The decided semantics below still govern every future surface — read them before touching anything.

**Detour (§4d) shipped via the PLACEHOLDER model, NOT the two-columns-on-the-linked-goal model §4d originally sketched.** A placeholder subtask carries `Planner.linkedItemId` (self-FK, SetNull) and becomes a pure redirect into the target root's leaves — this is more general (multi-reference, familiar subtask create/drag UX) and it required a full **flat-order engine rewrite**: the scheduler is now leaf-driven (candidate roots flattened to one leaf pool + a leaf precedence graph), with priority inheritance. See CLAUDE.md ("Detour" domain section + the engine summary's "Flat-order scheduling" bullet) and the deep-dive spine sections for the shipped design. Key files: [buildLeafGraph.ts](../utils/calendar-generation/helpers/Scheduler/buildLeafGraph.ts), [placeLeaf.ts](../utils/calendar-generation/helpers/Scheduler/placeLeaf.ts), [scheduleTasksAndGoals.ts](../utils/calendar-generation/helpers/Scheduler/scheduleTasksAndGoals.ts), [goalPageHandlers.ts](../utils/goalPageHandlers.ts) `getScheduledLeafSequence`, [detourLinks.ts](../utils/precedence/detourLinks.ts). The 2026-07-14 adversarial review's 10 findings are all fixed — notably: constrained-first leaf ordering restored, chain ends accumulate across split passes, zero-leaf queue members carry the gate bound forward, target exclusion follows ACTIVE hosts (completing a host frees the target), unready/completed targets are transparent to the splice, per-cap day-cap composition, placeholder children schedule after the splice, a root task keeps its own duration, and detour links contract host+target into one node for authoring-time cycle legality (this note's §4d decision (5), now shipped) — plus one review-missed pre-existing bug (split chunks escaped allowedTimes/earliestStartDate; the constraints map is now copied per chunk id). Deliberate v1 semantics: host earliestStartDate/allowedTimes do NOT bind spliced leaves; the day cap does. v1 gaps: assistant can't author detours (its op-time precedence validators can't see links either — the save-time defense catches contradictions); the subtasks tree renders a placeholder as an ordinary row (drawer reveals the link); leaf-level priority inheritance skips a leafless middle root (ordering-only); manual in-browser pass not yet run.

## Context

Goals conflate "completable outcome" with "ordered container". Work streams (e.g. a Work pipeline) need ordering without finitude; root-level items need prerequisite links ("get married" needs "find a girlfriend" AND "get an apartment") that no container expresses. Two concepts, deliberately distinct:

- **Queue (pipe)** — a first-class entity (NOT a plannerType): a persistent, user-authored, ordered list of references to existing root-level Planner items (tasks and goals). No endpoint; members exit in the order they entered. Managed at `/queues`.
- **Dependency** — a logical prerequisite edge between root-level items. Multi-predecessor, order among prerequisites irrelevant, authored in the item editor. No container entity — the connected component of the dependency graph IS the "project"; it is derived, never declared. No Project model, ever.

Both compile to one engine representation: `PrecedenceEdge { fromId, toId, source: "queue" | "dependency", queueId? }`. The engine gates each candidate on max end across all placed predecessors and never sees a cycle — legality is enforced at authoring time.

### Decided semantics (these govern all current and future surfaces)

1. **All members/successors schedule (full forecast)**, each bounded via the engine's existing `afterTime` seam — `findValidSlots` owns `effectiveAfter = max(afterTime, earliestStartDate chain)`, so precedence composes with per-item constraints for free. Never re-implement the max at a gate.
2. **Edge scope.** Both endpoints root-level, triaged, `task | goal`. Plans excluded (deferred). A planner belongs to at most one queue (DB unique on `QueueMember.plannerId`); dependencies make the merged graph a general DAG.
3. **Orthogonality.** Pipe membership and dependency are independent. A pipe stalls at a member with an unmet external dependency; successors never flow around a stalled member.
4. **Merged-graph validation, logical order.** Cycle checks run against dependency edges + each queue's FULL logical member order (completed/unready included — transparency affects gating, never legality). Validated on every mutation of either system. The engine never arbitrates.
5. **Redundant same-pipe dependencies are allowed**; contradictions are blocked with the cycle path shown.
6. **Transparency (engine gating).** Completed members/predecessors: transparent, no bound (both sources). Unready-goal predecessor: queue → silent chain-through; dependency → successor schedules unbounded + LOUD message. Asymmetry intentional.
7. **Permanent placement failure** of a predecessor → successors schedule anyway, unbounded, plus a message. Pure budget exhaustion emits a distinct "past the horizon" flavor (`SEQUENCE_PAST_HORIZON`) rather than pretending the sequence broke.
8. **Readiness gate (authoring-time).** A goal cannot be readied while any dependency predecessor is an unready goal ("Awaiting <title>", with per-blocker Ready/Disconnect shortcuts, no deep cascade); un-readying a goal a READY goal depends on is refused symmetrically. The engine keeps the defensive loud fallback (#6) for states that slip through.
9. **Queue's optional `categoryId` = inherited default** for members with no effective category of their own. Dependencies carry no category semantics.
10. **Future graph surfaces must require zero new rules** — they read the same data and write through the same validators/choke points.

---

## 1. What is implemented, and where

- **Data (Phase 1).** [prisma/schemas/models/queue.prisma](../prisma/schemas/models/queue.prisma) (`Queue`, `QueueMember` — the `@unique plannerId` IS the one-queue invariant), [dependency.prisma](../prisma/schemas/models/dependency.prisma) (`PlannerDependency`, immutable rows, `@@unique([predecessorId, successorId])`). Migration `20260713002513_add_precedence` applied to Neon dev. Types in [types/prisma.ts](../types/prisma.ts) (`Queue` includes nested `members` — the app/Redux shape).
- **Sync (Phase 2).** Fetch: [fetchCalendarData.ts](../actions/calendar-actions/fetchCalendarData.ts) + [fetchFreshState.ts](../actions/calendar-actions/fetchFreshState.ts). Diff: [compareCalendarData.ts](../utils/server-handlers/compareCalendarData.ts) — queue group strips members (stripTimeSlots pattern), members flattened by id, dependencies creates/deletes only. Handlers: [queueHandlers](../actions/calendar-actions/sync-handlers/queueHandlers.ts) / [queueMemberHandlers](../actions/calendar-actions/sync-handlers/queueMemberHandlers.ts) / [dependencyHandlers](../actions/calendar-actions/sync-handlers/dependencyHandlers.ts) (no bulkUpdate — immutable), spread after category+planner handlers in [syncCalendarData.ts](../actions/calendar-actions/syncCalendarData.ts). Redux: `calendarSourceSlice` gained `queues`/`dependencies` + `setQueues`/`setDependencies`; `useCalendarServerSync`/`useFetchCalendarData` carry both; `CalendarProvider` exposes them + `updateQueueArray`/`updateDependencyArray` (also in [useCalendarStateActions.ts](../hooks/useCalendarStateActions.ts)); [calendarThunks.ts](../redux/thunks/calendarThunks.ts) runs **central pruning** ([prunePrecedenceInputs.ts](../utils/precedence/prunePrecedenceInputs.ts) — identity-preserving on no-op) and passes both into engine options. [useManuallyRefreshCalendar.ts](../hooks/useManuallyRefreshCalendar.ts) passes them too.
- **Validation (Phase 3).** [utils/precedence/](../utils/precedence/): `types.ts` (PrecedenceEdge), `validationEdges.ts` (FULL-logical-order builder — the legality graph), `findCycle.ts` (findCycle + findCycleInGraph + three `wouldCreateCycle*` mutation shapes), `describeCycle.ts` (human path: titles + "(through the X queue)"), `readinessBlockers.ts`. [utils/queue-handlers/](../utils/queue-handlers/): `mutateQueueMembers.ts` (THE member-write choke point — add/reorder cycle-validated, remove free), `sortOrderKeys.ts` (fractional keys generalized to `{id, sortOrder}`), `queueLookups.ts` (`buildQueueCategoryByRootId` / `buildQueueByPlannerId` — the display seam).
- **Engine (Phase 4).** Input: `GenerateCalendarOptions.queues/dependencies` → `CalendarGenerationInput`; worker unchanged (plain JSON). [applyQueueCategoryInheritance.ts](../utils/calendar-generation/helpers/CalendarGenerator/applyQueueCategoryInheritance.ts) applied at the input boundary next to the isTriaged filter. [precedenceEdges.ts](../utils/calendar-generation/helpers/Scheduler/precedenceEdges.ts) (GATED builder — transparency applied; deliberately distinct from validationEdges, comments in both) + `buildPredecessorMap` built in CalendarGenerator Phase 4 as the third sibling map, hung on `SchedulingContext.predecessorMap`. [precedenceGate.ts](../utils/calendar-generation/helpers/Scheduler/precedenceGate.ts) (seedChainOutcomes / gateCandidate / isBlocked / recordSequenceBreaks) folded into [scheduleTasksAndGoals.ts](../utils/calendar-generation/helpers/Scheduler/scheduleTasksAndGoals.ts): blocked candidates skip passes and are excluded from watermark sizing; a resolving pass re-walks WITHOUT expanding; budget exhaustion marks outcome-less sources `failed/"horizon"` before the (gate-resolved) compromise pass. `scheduleSingleTask` gained `afterTime?` (threads to both `scheduleTask` and `scheduleSplitTask`); `scheduleGoal` gained `initialAfterTime?`, returns `lastPlacedEnd` and `allLeavesTooLarge` (now a permanentFailure — new behavior). Messages: `QUEUE_SEQUENCE_BROKEN` / `DEPENDENCY_BROKEN` / `SEQUENCE_PAST_HORIZON` in [EngineMessage.ts](../utils/calendar-generation/models/EngineMessage.ts) + [coalesceMessages.ts](../utils/calendar-generation/helpers/CalendarGenerator/coalesceMessages.ts) (`emitSequenceBreakMessages`) + [renderEngineMessage.ts](../utils/renderEngineMessage.ts) (`EngineMessageLookups.queueById`; caller updated in calendar/page.tsx).
- **UI queues (Phase 5).** [app/(protected)/queues/](../app/(protected)/queues/) — categories-page skeleton clone: `QueueRail` (drag-reorder, dense-int renumber), `QueueMemberList` (RolesStep drag grammar, fractional keys, completed dimmed/struck, unready-goal hint, inherited CategoryBadge dimmed), `AddMemberModal` (SearchPalette fork, candidates filtered root/triaged/task|goal/uncompleted/not-in-any-queue/acyclic), queue header title + category Combobox + delete. All writes through the choke point; refusals show `describeCycle` in the error banner. Nav entry (lucide `ListOrdered`, `NAV_ITEMS` only). `getEffectiveCategoryId` gained optional `queueCategoryByRootId` — swept: dashboard `buildTodayAgenda`/`buildUncompletedItems`, `ItemDetailLayout`, `buildInheritedLocationMap` (via CalendarProvider). "In queue" card in item-detail SideCards.
- **UI dependencies + readiness (Phase 6).** `DependenciesCard` in [SideCards.tsx](../app/(protected)/items/[id]/_components/SideCards/SideCards.tsx) (Depends on: chips + Combobox picker, cycle-blocked candidates annotated + commit hard-check with path; Required by: read-only reverse list — the minimal "viewed somewhere" until the graph view). Readiness gate in [ItemDetailLayout.tsx](../app/(protected)/items/[id]/_components/ItemDetailLayout/ItemDetailLayout.tsx) + [DependencyGatePopover.tsx](../app/(protected)/items/[id]/_components/ItemDetailLayout/DependencyGatePopover.tsx) (both directions, per-row gated shortcuts, in-place refusal messages). `toggleGoalIsReady`/`setGoalIsReady` stay dumb stampers; the only live toggle call sites are gated (audited — ItemContext.toggleReady has no other consumers).
- **Assistant containment (Phase 7) — SUPERSEDED by full read/write (2026-07-13).** The isReady clamp survives (now exported as `clampReadinessAgainstDependencies` and re-run after the precedence apply), but the assistant is no longer contained: it has a full precedence draft domain à la templates/windows. Shared contract [draftPrecedence.ts](../utils/draft/draftPrecedence.ts) (`DraftPrecedenceState` — member order is array position, dependencies are pairs; normalize/equality/prune/edges), deterministic ops [draftPrecedenceOps.ts](../utils/draft/draftPrecedenceOps.ts) (queue CRUD + member add/move/remove + dependency add/remove, all cycle-validated through the SHARED findCycle validators, refusals carry the describeCycle-style path), 8 route tools + prompt section + SSE `precedence` full-state event (route prunes precedence against the forest at request start and after every forest mutation), a Queues review tab ([PrecedenceView](../components/draft/PrecedenceView/PrecedenceView.tsx) + [diffDraftPrecedence.ts](../utils/draft/diffDraftPrecedence.ts)), and the save path: `applyDraftForestToPlanner` reports minted root ids through `rootIdMap`, then [applyDraftPrecedence.ts](../utils/draft/applyDraftPrecedence.ts) replays the assistant's deltas onto the live arrays (draft-goal ids remapped, no-resurrection, user's concurrent placement wins one-queue conflicts, final merged-state cycle defense drops assistant artifacts only), then the clamp re-runs with the final edges. `updateAll` grew `queues`/`dependencies` params (before `options`). Tests: `__tests__/draft/draftPrecedenceOps.test.ts` + `applyDraftPrecedence.test.ts`.
- **Tests (Phase 8).** `__tests__/calendar-generation/`: `queue-sequence` (7), `dependency-gate` (7), `precedence-constraints-compose` (4), `queue-category-inheritance` (2 + unit cases), `precedence-edges`; `__tests__/utils/precedence/`: `findCycle`, `readinessBlockers`, `prunePrecedenceInputs`; `__tests__/utils/renderEngineMessage.test.ts` (null-safety, deleted entities). Full suite 43/43, 409 tests green; `pnpm lint`, `pnpm type-check`, `pnpm build` clean. CLAUDE.md updated (domain section "Precedence", directory map, nav table, migration history, tests list).

## 2. Deviations from the original plan / gotchas

- **The keyed-object refactor of the sync seam HAPPENED** (the plan's "preferred" option): `compareData`/`handleServerTransaction` now take a single `SyncInputs` object of `{current, previous}` groups instead of ~20 positional params. Any future sync group extends `SyncInputs` + `DatabaseChanges` + `runSync`'s literal.
- **Watermark capacity exclusion (new engine behavior, load-bearing).** `scheduleTasksAndGoals` now also excludes from watermark sizing any candidate whose `effectiveCandidateDuration` exceeds `maxEffectiveCapacityFor` (horizon-independent weekly ceiling). Without it, a TOO_LARGE candidate pinned `biggestFit < biggestRemaining` and burned all 12 expansions before the walk could fail it — which misclassified precedence breaks as `"horizon"` instead of `"failed"` (and was a pre-existing waste for plain too-large tasks). Covered by queue-sequence test 5.
- **Dependency picker degrades "disabled with reason" to "annotated + hard-check"**: `Combobox` has no per-option disable, so cycle-blocked candidates are listed with "— would create a loop" and the commit refuses with the full `describeCycle` path. The one-click fix offer (semantics #5: "move D ahead of B") is NOT shipped — fast-follow as planned.
- **DependencyGatePopover uses Radix Popover** (the Combobox precedent), not `usePopoverPosition`.
- **Test-pattern split**: queue-sequence + dependency-gate use the live-data fixture as planned; precedence-constraints-compose + queue-category-inheritance are hand-built minimal geometry (split-task/goal-day-cap precedent) because their assertions need controlled slot fabric.
- **Identity discipline is everywhere and tested** — prune helpers, `applyQueueCategoryInheritance`, `mutateQueueMembers.removeQueueMember` all return the same reference on no-op. Keep it that way; phantom diffs make second-window syncs permanently stale.
- Queue rows created client-side carry the provider `userId`; member rows are minted in the choke point with fresh uuid + timestamps. Queue edits do NOT restamp `updatedAt` client-side (categories precedent); member reorders do (harmless, value-based diff).

## 3. Outstanding verification (manual, in-browser — not yet run)

Type-check/tests/build/migration are done. Still unexercised in the running app: create a queue and confirm sequential placement + drag-reorder regen + completed-member pass-through; strict-category inheritance on a categoryless member; oversized member → console card + dismiss surviving regen; the "Awaiting…"/"Required by…" gate flows and three-dot shortcuts; picker cycle refusal both plain and cross-system (pipe named in the path, reorder-then-insert-then-blocked-reorder round trip); pipe visibly stalling on an external dependency; two-tab stale adoption + idle-regen-diffs-empty (network tab); library-delete cascading member+edges cleanly; constraint composition spot-checks (earliest-start-after-bound, split-predecessor last chunk, day-capped predecessor).

Graph view (§4b, 2026-07-14): spans/lanes/rows against real engine output at all three zoom levels; drag-to-link commit + regen (connector appears, successor moves); loop drop refused with the path in the banner; lane drag-reorder round trip (including a cross-system cycle refusal); connector click → remove → regen; "out of order" dashed connector on a broken sequence; docked unscheduled nodes; show-completed toggle; sticky lane labels + axis while scrolling; node click-through to item detail without triggering a drag.

---

## 4. Hand-off: not-yet-implemented features

### 4a. First commit of any new view: lift the shared lookup seam — DONE (2026-07-14)

`buildQueueCategoryByRootId` / `buildQueueByPlannerId` ([queueLookups.ts](../utils/queue-handlers/queueLookups.ts)) are now memoized ONCE on the CalendarProvider context (`queueCategoryByRootId` / `queueByPlannerId`); the former per-call-site copies (dashboard hook, ItemDetailLayout, SideCards InQueueCard) read the context values. New views (graph / file / mind-map) must consume these — never build page-local copies.

### 4b. Directional Graph view (TODO's "directional graph … like a timeline") — DONE (2026-07-14)

Shipped exactly as scoped — zero validator/sync/engine changes; every write goes through the existing seams. Route [app/(protected)/graph/](../app/(protected)/graph/) (nav entry "Graph", lucide `Waypoints`, NAV_ITEMS only): pure view model in `_lib/graphModel.ts` (`buildRootSpans` — planner events only, event → planner via `plannerIdFromEventId`, root via parent walk; `buildGraphLanes`; `layoutGraph` — greedy per-lane row packing; `buildGraphTicks` — day/week/month granularity by pxPerDay), rendering + interactions in `_components/GraphCanvas/`. Unit tests in `__tests__/graph/graphModel.test.ts`.

Implementation decisions on the open points (revised same day after first in-browser feedback):

- **Lane content**: one lane per queue — members weave OVER-UNDER in member order (two alternating rows; single-row was tried first, but back-to-back placements touch and read as one bar). Non-queue root goals and dependency-endpoint tasks group under an **indented category tree** below the queues: one lane per category with items (greedy row packing), ordered by a depth-first walk (headings indented per depth, CategoryDot color); ancestor categories whose items all live deeper render as compact header-only context lanes (no separator/count); uncategorized items land in a trailing lane. Roots carry categoryId directly (root-only invariant), so grouping needs no parent-chain walk. Plain unlinked tasks are deliberate noise-exclusions. Completed nodes hidden by default behind a header toggle. Full member order carried separately for drop-index math, QueueMemberList parity.
- **Queue color**: new nullable `Queue.color` column (migration `add_queue_color`, applied to Neon dev), picked via `PopoverColorPicker` in the queues-page header; falls back to the queue's category color. Tints the lane background (7% color-mix).
- **Unscheduled endpoints** (unready goals, placement failures) dock past the domain's right edge under a "Not scheduled" axis label — they stay in content coordinates so connectors attach normally.
- **Connectors**: dependencies are arrowed beziers (click → inspect/remove chip); queue order is IMPLICIT (lane membership + left-to-right time — colored chain arrows were tried and read as clutter), so consecutive members draw a connector only when out of order; a successor placed before its predecessor's end renders dashed in the error color ("out of order" — the engine's loud fallback made visible).
- **Nodes are pills** (`radii.pill`, min width = node height so a fully squished node clamps to a perfect circle) with the item's accent as a 3px LEFT BORDER — a tinted-fill variant was tried and reverted on feedback — and **scale with the zoom** (height 18–30px, log-mapped over the slider's px-per-day range — `nodeHeightForZoom`). Title tiers by available space: as much of the title as fits renders with an ellipsis (tighter padding under 72px wide); below 40px just the first letter while node height ≥22px; below that no text. Hovering ANY node shows a floating detail badge (title / scheduled window / duration) that fully replaces the native tooltip (no `title` attr), toggleable via the header "Hover labels" switch (chosen over permanent under-node labels, which would collide with the packed row pitch).
- **Drag-to-link works from BOTH edges**, pointer-capture based (live line from the handle): the right handle makes the source the predecessor, the left handle makes the drop target the predecessor. Validated per hovered target via `wouldCreateCycleAddingDependency` with a short reason chip; an attempted loop drop banners `describeCycle`'s full path. Lane reorder is the shared HTML5 drag grammar rotated horizontal (before/after = left/right inset lines) through `reorderQueueMember`.
- **Zoom is a logarithmic slider** (6–960 px/day — the top reaches hour granularity, thumb in theme gray) that keeps the viewport's center time fixed while zooming — pinned to the domain start instead while the viewport touches the far left; first mount anchors on "now". **Wheel remap on the canvas**: plain wheel pans horizontally, shift+wheel scrolls vertically, ctrl/meta+wheel (and trackpad pinch) zooms anchored at the cursor — native non-passive listener, since React's root wheel handlers are passive and would ignore preventDefault. **Time markers are per-unit** (hour/day/week/month), toggleable via the header "Markers" popover (`MarkerMenu`); each unit auto-hides while illegible (lines <10px apart, labels per-unit thresholds), a boundary day belongs to the coarsest visible unit, and hour ticks are viewport-windowed (quantized scroll window) so a long domain at hour zoom never floods the DOM. **`layout.scheduleEndX`** marks the end of the engine's placements — a dashed delimiter plus a faint ink wash over the uncalculated beyond (the dock sits inside the wash). The canvas fills the card height — the space under the last lane reads as an unnamed row, so every named lane ends with a bottom border.
- Node category accents use the CalendarProvider `queueCategoryByRootId` seam (4a) — no page-local lookups.
- Manual in-browser pass partially run (first-pass feedback incorporated); see §3 for the remaining checklist.

### 4c. File-view and Mind-map view (TODO: categories + sub-categories + items together)

Category-tree surfaces, orthogonal to precedence — but they must render queue membership and effective category with the SAME lookups (4a), not their own resolution, so all views plus item detail agree with the engine's `applyQueueCategoryInheritance`.

### 4d. Goal linked into another goal as a detour (TODO's "goal as subtask") — IMPLEMENTED 2026-07-13

**Shipped via the placeholder model (see the STATUS note at the top), not the columns-on-the-linked-goal design sketched below.** The analysis below is preserved for context; the shipped semantics live in CLAUDE.md.

NOT reparenting and NOT containment. The linked goal stays a separate root — own category, own constraints, own readiness, own item page — and a link marks a position inside the host goal's tree. Scheduling is a **traversal-time splice**: the host's leaf enumeration inlines the linked goal's sorted bottom layer at the link position, and the existing `goalAfterTime` chaining does the rest. Zero new gate mechanics; the edit lives where `scheduleGoal` enumerates leaves (`getSortedTreeBottomLayer` consumer), not in the precedence gate.

- Falls out free: `buildPlannerCategoryMap` / `buildPlannerConstraintsMap` walk the real `parentId` chain, so the linked goal's leaves keep ITS category and constraints while riding the host's pacing; the root stays root, so central pruning never bites.
- Data model (decided): NOT redirect pointers on neighboring tasks (the retired `dependency` linked-list column reborn — see `drop_planner_dependency`), and NOT a placeholder child row (pseudo-plannerType leaks into every planner consumer). One fact on the LINKED goal: two nullable Planner columns — `detourHostId String?` (FK → Planner, `onDelete: SetNull`) + `detourSortOrder Float?` (position on the host parent's fractional sibling axis; inert once `detourHostId` is null). The scalar IS the one-link invariant; "is linked" = `detourHostId != null`; the columns ride the existing planner sync/diff/spread-preserve machinery with zero new plumbing (the `earliestStartDate`/`allowedTimes` reasoning). Root-goals-only, stale values on retyped/nested rows inert (`maxMinutesPerDay` pattern). Real cost: sibling enumeration merges two sources on one fractional axis (own children by `sortOrder` + incoming detours by `detourSortOrder`) — build the detours-by-host index in the memoized per-array pass `getSortedTreeBottomLayer` already runs. Central pruning gains `pruneDetours`: clear the pair when the host node no longer resolves into a triaged root goal's tree (retype/untriage; deletes covered by SetNull).
- Decisions the implementation owes: (1) a linked goal stops being an independent candidate while linked (else its leaves place twice) — constrains the SPLICE only, never prerequisite fan-in/out ("needed by many things" stays dependency edges); queue membership simplest as mutually exclusive with being linked; (2) day caps compose pointwise-min across host and linked goal (the split × goal-cap budget seam is the template); (3) transparency: completed linked goal → chain passes through; unready → pick skip-silently (queue semantics) vs stall-loud (dependency semantics); (4) a linked goal with its own dependency predecessors makes the host walk consult the gate MID-walk — v1 can reject links on goals with incoming edges; (5) validation: contract host + linked goal into ONE node (transitively across link chains) in the merged graph — this is why `PrecedenceEdge.source` and the validation builder stayed extensible rather than a closed union.
- For plain "goal X needs goal Y" with no interleaving, the shipped dependency edge is still the answer — the detour is for when Y's work belongs INSIDE X's sequence.

### 4e. Subtree-level dependencies — decided against (2026-07-13), possibly permanently

Root-only endpoints keep dependencies a top-level concept (matching root-only `categoryId`/`color`/queue-membership) and the authoring surface holdable-in-head. Preserved analysis, split by which SIDE goes interior:

- Interior SUCCESSORS only (mid-chain leaf gating on external roots) are **position-independent**: attach-time validation suffices, subtask reorders stay free; engine cost is the same mid-walk gate consult the detour's decision (4) needs. Safe to add later without taxing any drag surface.
- Interior PREDECESSORS are **position-dependent**: every goal's internal leaf order joins the merged cycle graph, so EVERY subtask drag becomes a cycle-validated mutation across the library tree, subtasks page, and assistant move ops. That reorder tax plus conceptual load is the reason for the skip.
- Workarounds inside root-only scope: depend on the whole goal (coarser), or promote the prerequisite subtask to its own root and depend on that (composes with a detour re-splicing it into its goal's chain).

### 4f. Smaller deferred items

- Queue-conflict **one-click fix offers** (semantics #5: compute "move D ahead of B" / "disconnect" from the cycle path) — the picker/reorder surfaces currently block-and-explain only.
- ~~**Assistant read/write of precedence**~~ — SHIPPED 2026-07-13 (see the Phase 7 entry in §1). Remaining assistant gap: the manual in-browser pass for the new tools (create/reorder/remove through chat, cycle refusal relayed in prose, draft-goal-into-queue save round trip) has not been run.
- **Plans as members/endpoints**; **named clusters** (labels on derived dependency components — still no Project entity).
